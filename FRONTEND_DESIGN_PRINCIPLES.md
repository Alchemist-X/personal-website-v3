# 前端设计原则

本文档总结当前项目的前端设计原则，便于后续扩展或维护时保持一致的视觉和交互体验。

## 1. 设计目标与风格
- **CLI/终端美学优先**：整体视觉围绕“终端窗口 + 命令行”风格展开。
- **信息分块清晰**：以窗口/卡片为单位组织内容，强调可扫描性。
- **内容优先、装饰克制**：动效和装饰仅用于强化层级与反馈，不喧宾夺主。

## 2. 色彩与主题
- **主题驱动**：所有基础颜色来自 CSS 变量（`--terminal-*`、`--page-*`）。
- **深浅主题一致**：默认深色在 `:root`，浅色通过 `html.light` 覆盖变量，语义色在两套主题下保持一致。
- **避免硬编码颜色**：除提示色（如命令提示黄）外，优先使用 token/变量。

## 3. 排版与可读性
- **等宽字体为主**：全站默认使用 monospace，强化终端风格。
- **清晰层级**：标题尺寸明显递进，正文保持舒适行距与对比度。
- **强调色用于链接与强调**：链接默认使用 `--link-color`，保持统一。

## 4. 布局与响应式
- **网格化布局**：首页采用自适应网格（masonry 风格），其他页面用常规栅格。
- **内容宽度控制**：使用 `max-w-*` 控制主内容宽度，避免超宽阅读负担。
- **底部固定导航**：导航固定在底部，内容区统一使用 `pb-24` 留出空间。

## 5. 组件与复用
- **TerminalWindow**：作为信息块的基础容器，统一圆角、边框、标题栏与阴影。
- **CommandLine**：统一命令提示符样式，强化终端语境。
- **NavBar**：底部固定、图标化导航，激活态用反转色突出位置。
- **卡片模式复用**：Blog/Playground/Misc 等页面沿用卡片结构与视觉语义。

## 6. 交互与动效
- **轻量过渡**：hover/颜色/边框变化以 0.3s 左右过渡为主。
- **节奏感动效**：`fade-in` 用于窗口/模块出现；`typing` 仅在需要“终端”氛围时使用。
- **不影响阅读**：动效不应改变布局高度，避免内容跳动。

## 7. 内容组织方式
- **命令驱动的段落结构**：内容块以“命令 + 输出”的节奏呈现。
- **列表化表达**：使用列表/标签辅助扫描信息（例如 tags、研究方向）。
- **局部聚焦**：一个窗口/卡片只承担一个主题，避免混杂。

## 8. 资产与媒体
- **静态资源集中**：图片/简历等放在 `public/`，使用根路径引用。
- **图片自适应**：常用 `object-cover` 保持比例与裁切一致。
- **占位清晰**：未提供资源时保留明确占位文本，避免空白。

## 9. 可维护性与一致性
- **Tailwind 为主、少量全局样式**：基础风格在 `index.css`，其余尽量用 utility class。
- **语义化变量命名**：样式变量以“用途”命名，便于跨主题复用。
- **统一组件风格**：新增组件应复用现有 token/交互模式，避免风格漂移。

